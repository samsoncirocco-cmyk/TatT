# Dependencies
/node_modules
/.pnp
.pnp.js

# Testing
/coverage

# Next.js
/.next/
/out/

# Production
/build

# Misc
.DS_Store
*.pem

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Local .env files
.env*.local
.env
.env.master

# Claude Code — .claude/settings.json + .claude/hooks/ are tracked on purpose
# (shared agent config). Agent worktrees and per-user overrides are not.
.claude/worktrees/
.claude/settings.local.json

# Editor directories and files
.vscode
.idea
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Temporary files
/artifacts
/tmp
/temp
*.tmp
*.temp
*.log


# Vercel
.vercel/

# Firebase
firebase-admin-key.json
gcp-service-account-key.json
# gcs-cors.json is a committed deploy artifact (referenced by docs/gcs-setup.md,
# docs/GCP_MIGRATION_PROGRESS.md) — tracked intentionally, do not ignore.
TATT_ENV_REFERENCE.md
firebase-service-account.json
*-service-account-key.json
*-service-account.json
*service-account*.json

# TypeScript
tsconfig.tsbuildinfo
*.tsbuildinfo
# Crawler runtime checkpoints — regenerated on every run, never commit
# (path lives under src/, so match at any depth)
**/data_acquisition/output/
# Artist discovery/refresh runtime artifacts contain scraped profile data and
# mutable checkpoints. Review them locally; never mix them into source PRs.
/data/discovery/
/data/enrichment/instagram/apify-profiles/
/data/enrichment/instagram/refresh-status.json
/data/enrichment/instagram/refresh-audit.jsonl
/data/enrichment/instagram/apify-run-report.json
.env.firebase

# Python
__pycache__/
*.pyc

# Expo
.expo/
Neo4j-*.txt
.vercel
.env*
