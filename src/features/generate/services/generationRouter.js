// Compatibility re-export for legacy imports from `src/features/generate/...`.
// routeGeneration now lives in the generation module (pure routing helper).
export { routeGeneration } from '@/services/generation';
