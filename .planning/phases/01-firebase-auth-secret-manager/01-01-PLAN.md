---
phase: 01-firebase-auth-secret-manager
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/firebase-client.ts
  - src/hooks/useAuth.ts
  - src/components/auth/AuthProvider.tsx
  - src/components/auth/LoginForm.tsx
  - src/components/auth/SignupForm.tsx
  - src/app/(auth)/login/page.tsx
  - src/app/(auth)/signup/page.tsx
  - src/app/layout.tsx
autonomous: true
user_setup:
  - service: firebase-auth
    why: "Firebase Authentication must be enabled in the Firebase Console"
    env_vars:
      - name: NEXT_PUBLIC_FIREBASE_API_KEY
        source: "Firebase Console -> Project Settings -> General -> Web app config"
      - name: NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN
        source: "Firebase Console -> Project Settings -> General -> Web app config"
      - name: NEXT_PUBLIC_FIREBASE_PROJECT_ID
        source: "Firebase Console -> Project Settings -> General -> Web app config"
    dashboard_config:
      - task: "Enable Email/Password sign-in provider"
        location: "Firebase Console -> Authentication -> Sign-in method -> Email/Password -> Enable"

must_haves:
  truths:
    - "User can sign up with email and password"
    - "User can log in with existing credentials"
    - "User session persists across browser refresh and tab close"
    - "Auth state loading shows spinner, not flash of unauthenticated content"
  artifacts:
    - path: "src/lib/firebase-client.ts"
      provides: "Firebase client SDK initialization with browserLocalPersistence"
      exports: ["app", "auth"]
    - path: "src/hooks/useAuth.ts"
      provides: "Client-side auth state hook"
      exports: ["useAuth"]
    - path: "src/components/auth/AuthProvider.tsx"
      provides: "Auth context provider wrapping app"
      exports: ["AuthProvider", "useAuthContext"]
    - path: "src/app/(auth)/login/page.tsx"
      provides: "Login page with form"
    - path: "src/app/(auth)/signup/page.tsx"
      provides: "Signup page with form"
  key_links:
    - from: "src/app/layout.tsx"
      to: "src/components/auth/AuthProvider.tsx"
      via: "AuthProvider wraps children in root layout"
      pattern: "<AuthProvider>"
    - from: "src/components/auth/LoginForm.tsx"
      to: "src/lib/firebase-client.ts"
      via: "signInWithEmailAndPassword using auth instance"
      pattern: "signInWithEmailAndPassword"
    - from: "src/hooks/useAuth.ts"
      to: "src/lib/firebase-client.ts"
      via: "onAuthStateChanged listener"
      pattern: "onAuthStateChanged"
---

<objective>
Set up Firebase Authentication client-side infrastructure: SDK initialization, auth state management, signup/login pages, and AuthProvider context.

Purpose: Enables user account creation and sign-in (AUTH-01) with session persistence across refresh/tab close (AUTH-02). This is the foundation all other auth work depends on.

Output: Working signup and login flow with persistent sessions. Users can create accounts and sign in.
</objective>

<execution_context>
@/Users/maryobrien/.claude/get-shit-done/workflows/execute-plan.md
@/Users/maryobrien/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-firebase-auth-secret-manager/01-RESEARCH.md
@src/services/firebase-match-service.ts
@src/app/layout.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Firebase client initialization and auth hooks</name>
  <files>
    src/lib/firebase-client.ts
    src/hooks/useAuth.ts
    src/components/auth/AuthProvider.tsx
  </files>
  <action>
    1. **src/lib/firebase-client.ts** — Initialize Firebase client SDK:
       - Import `initializeApp` from `firebase/app` and `getAuth`, `setPersistence`, `browserLocalPersistence` from `firebase/auth`
       - Use `NEXT_PUBLIC_FIREBASE_API_KEY`, `NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN`, `NEXT_PUBLIC_FIREBASE_PROJECT_ID` env vars for config
       - Also include `NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET`, `NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID`, `NEXT_PUBLIC_FIREBASE_APP_ID` if present
       - Call `setPersistence(auth, browserLocalPersistence)` to survive tab close (AUTH-02)
       - Export `app` and `auth` instances
       - NOTE: The existing `firebase-match-service.ts` has its own `initializeApp` — do NOT modify it. The Firebase SDK handles duplicate init gracefully with `getApps()` check. Add `getApps().length === 0` guard in firebase-client.ts.

    2. **src/hooks/useAuth.ts** — Create auth state hook:
       - Use `onAuthStateChanged` from `firebase/auth` with `useState` + `useEffect`
       - Track `user` (User | null), `loading` (boolean), `error` (Error | null)
       - Return `{ user, loading, error, isAuthenticated: !!user }`
       - Add `signUp(email, password)` function using `createUserWithEmailAndPassword`
       - Add `logIn(email, password)` function using `signInWithEmailAndPassword`
       - Add `logOut()` function using `signOut`
       - Handle Firebase error codes with user-friendly messages: `auth/email-already-in-use`, `auth/weak-password`, `auth/user-not-found`, `auth/wrong-password`, `auth/invalid-email`
       - Add `getIdToken()` async function that returns `user.getIdToken()` for later use by API calls

    3. **src/components/auth/AuthProvider.tsx** — Create auth context provider:
       - Create React context with auth state from `useAuth` hook
       - Export `AuthProvider` component and `useAuthContext` hook
       - `AuthProvider` wraps children, provides auth state via context
       - This avoids prop drilling — any component can call `useAuthContext()`
       - Mark as `'use client'` directive (uses React state/effects)
  </action>
  <verify>
    - `npm run build` completes without TypeScript errors
    - Verify firebase-client.ts exports `app` and `auth`
    - Verify useAuth.ts exports `useAuth` with all required functions
    - Verify AuthProvider.tsx exports `AuthProvider` and `useAuthContext`
  </verify>
  <done>
    Firebase client initialized with LOCAL persistence, useAuth hook provides signup/login/logout functions with error handling, AuthProvider context wraps app state.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create signup and login pages with AuthProvider integration</name>
  <files>
    src/components/auth/LoginForm.tsx
    src/components/auth/SignupForm.tsx
    src/app/(auth)/login/page.tsx
    src/app/(auth)/signup/page.tsx
    src/app/layout.tsx
  </files>
  <action>
    1. **src/components/auth/SignupForm.tsx** — Create signup form component:
       - `'use client'` directive
       - Form fields: email (type="email"), password (type="password"), confirm password
       - Client-side validation: email format, password min 6 chars, passwords match
       - Call `signUp(email, password)` from `useAuthContext()` on submit
       - Show loading state during signup (disable button, show spinner)
       - Display error messages from Firebase (email already in use, weak password, etc.)
       - On success, redirect to `/dashboard` using `useRouter()` from `next/navigation`
       - Link to login page: "Already have an account? Log in"
       - Style with existing Tailwind classes: glass-panel aesthetic, Ducks Green (#154733) accents, Ducks Yellow (#FEE123) for CTA button, font-display for headings, font-sans for body

    2. **src/components/auth/LoginForm.tsx** — Create login form component:
       - `'use client'` directive
       - Form fields: email, password
       - Call `logIn(email, password)` from `useAuthContext()` on submit
       - Loading state, error display (same patterns as signup)
       - On success, redirect to `/dashboard`
       - Link to signup: "Don't have an account? Sign up"
       - Same styling as SignupForm for consistency

    3. **src/app/(auth)/login/page.tsx** — Login page:
       - Import and render LoginForm
       - Centered layout with max-w-md container
       - TatTester logo/branding at top
       - If user is already authenticated (from context), redirect to /dashboard

    4. **src/app/(auth)/signup/page.tsx** — Signup page:
       - Import and render SignupForm
       - Same layout pattern as login page
       - If user is already authenticated, redirect to /dashboard

    5. **src/app/layout.tsx** — Wrap app with AuthProvider:
       - Import `AuthProvider` from `@/components/auth/AuthProvider`
       - Wrap `{children}` with `<AuthProvider>` inside the existing layout
       - Keep all existing layout structure intact (fonts, metadata, etc.)
       - AuthProvider must be inside the `<body>` tag
  </action>
  <verify>
    - `npm run build` completes without errors
    - Verify /login and /signup routes exist (check page.tsx files created)
    - Verify layout.tsx includes AuthProvider wrapping children
    - Visually verify: pages should render with glass-panel styling and proper form fields
  </verify>
  <done>
    Signup and login pages render with forms, validation, error handling, and redirect on success. AuthProvider wraps the app in root layout. User can navigate to /login and /signup.
  </done>
</task>

</tasks>

<verification>
1. `npm run build` passes with zero TypeScript errors
2. Navigate to /signup — form renders with email, password, confirm password fields
3. Navigate to /login — form renders with email, password fields
4. AuthProvider wraps children in layout.tsx
5. Firebase client uses browserLocalPersistence (grep for `browserLocalPersistence` in firebase-client.ts)
</verification>

<success_criteria>
- Firebase Auth SDK initialized with LOCAL persistence (survives tab close)
- Signup form creates new user accounts via Firebase
- Login form authenticates existing users
- Auth state available via useAuthContext() throughout the app
- Loading state prevents flash of unauthenticated content
- Error messages displayed for invalid credentials, duplicate emails, weak passwords
</success_criteria>

<output>
After completion, create `.planning/phases/01-firebase-auth-secret-manager/01-01-SUMMARY.md`
</output>
