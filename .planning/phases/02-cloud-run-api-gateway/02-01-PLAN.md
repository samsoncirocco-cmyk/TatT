---
phase: 02-cloud-run-api-gateway
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Dockerfile
  - .dockerignore
  - next.config.ts
  - src/app/api/v1/generate/route.ts
  - src/app/api/v1/council/enhance/route.ts
  - src/app/api/v1/match/semantic/route.ts
  - src/app/api/predictions/route.ts
  - src/app/api/predictions/[id]/route.ts
  - src/app/api/health/route.ts
autonomous: true

must_haves:
  truths:
    - "Next.js builds in standalone mode producing a self-contained output directory"
    - "Docker image builds successfully with multi-stage pattern under 200MB"
    - "No API routes declare Edge Runtime — all use Node.js runtime"
  artifacts:
    - path: "Dockerfile"
      provides: "Multi-stage Docker build for Cloud Run"
      contains: "FROM node:20-alpine"
    - path: ".dockerignore"
      provides: "Docker context exclusions"
      contains: "node_modules"
    - path: "next.config.ts"
      provides: "Standalone output configuration"
      contains: "output: 'standalone'"
  key_links:
    - from: "Dockerfile"
      to: "next.config.ts"
      via: "npm run build produces .next/standalone"
      pattern: "COPY.*\\.next/standalone"
---

<objective>
Prepare the Next.js application for Cloud Run deployment by creating a production Dockerfile with multi-stage build, enabling standalone output mode, and removing all Edge Runtime declarations from API routes.

Purpose: Cloud Run requires a Docker container. Standalone mode reduces image size by ~70%. Edge Runtime declarations are incompatible with GCP libraries and misleading on Cloud Run (which always uses full Node.js).

Output: Dockerfile, .dockerignore, updated next.config.ts, all API routes on Node.js runtime.
</objective>

<execution_context>
@/Users/maryobrien/.claude/get-shit-done/workflows/execute-plan.md
@/Users/maryobrien/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-cloud-run-api-gateway/02-RESEARCH.md
@next.config.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Standalone build config + Dockerfile + .dockerignore</name>
  <files>next.config.ts, Dockerfile, .dockerignore</files>
  <action>
    1. Update `next.config.ts` to add `output: 'standalone'` to the NextConfig object. Keep all existing webpack config (client-side fallbacks, server-only aliases) unchanged. Do NOT remove `typescript.ignoreBuildErrors: true` or `turbopack: {}`.

    2. Create `Dockerfile` with three-stage multi-stage build:
       - **Stage 1 (deps):** `FROM node:20-alpine AS deps`, copy package*.json, run `npm ci`
       - **Stage 2 (builder):** `FROM node:20-alpine AS builder`, copy package*.json, `npm ci`, copy all source, run `npm run build`
       - **Stage 3 (runner):** `FROM node:20-alpine AS runner`, set `NODE_ENV=production`, create non-root user (nodejs group gid 1001, nextjs user uid 1001), copy `--from=builder /app/.next/standalone ./`, copy `--from=builder /app/.next/static ./.next/static`, copy `--from=builder /app/public ./public` (with chown nextjs:nodejs). Set `USER nextjs`, `EXPOSE 3000`, env `PORT=3000` and `HOSTNAME="0.0.0.0"`, CMD `["node", "server.js"]`.

    3. Create `.dockerignore` excluding: `node_modules`, `.next`, `.git`, `.env`, `.env.*`, `*.md`, `.planning`, `docs`, `.github`, `coverage`, `.vscode`, `Dockerfile`, `.dockerignore`, `vitest.config.*`, `eslint.config.*`.
  </action>
  <verify>
    - `grep "output.*standalone" next.config.ts` returns a match
    - `grep "FROM node:20-alpine" Dockerfile | wc -l` returns 3
    - `grep "USER nextjs" Dockerfile` returns a match
    - `.dockerignore` file exists with node_modules listed
  </verify>
  <done>
    next.config.ts has output: 'standalone'. Dockerfile has 3-stage build with non-root user. .dockerignore excludes dev artifacts.
  </done>
</task>

<task type="auto">
  <name>Task 2: Remove Edge Runtime declarations from all API routes</name>
  <files>
    src/app/api/v1/generate/route.ts
    src/app/api/v1/council/enhance/route.ts
    src/app/api/v1/match/semantic/route.ts
    src/app/api/predictions/route.ts
    src/app/api/predictions/[id]/route.ts
    src/app/api/health/route.ts
  </files>
  <action>
    Find all API route files with `export const runtime = 'edge'` and replace with `export const runtime = 'nodejs'`. Also add `export const dynamic = 'force-dynamic'` to each route that didn't already have it (API routes should not be statically optimized).

    Specific files that currently declare `runtime = 'edge'`:
    - `src/app/api/v1/generate/route.ts` — change to 'nodejs'
    - `src/app/api/v1/council/enhance/route.ts` — change to 'nodejs'
    - `src/app/api/v1/match/semantic/route.ts` — change to 'nodejs'
    - `src/app/api/predictions/route.ts` — change to 'nodejs'
    - `src/app/api/predictions/[id]/route.ts` — change to 'nodejs'
    - `src/app/api/health/route.ts` — change to 'nodejs'

    Files that already have `runtime = 'nodejs'` should be left as-is but get `export const dynamic = 'force-dynamic'` if they don't have it.

    Do NOT modify any business logic, imports, or request/response handling in these files. Only touch the runtime/dynamic exports.
  </action>
  <verify>
    - `grep -r "runtime = 'edge'" src/app/api/` returns zero results
    - `grep -r "runtime = 'nodejs'" src/app/api/ | wc -l` matches total route count
    - `npm run build` succeeds (or at least type-check passes for these files)
  </verify>
  <done>
    Zero API routes declare Edge Runtime. All routes declare `runtime = 'nodejs'` and `dynamic = 'force-dynamic'`.
  </done>
</task>

</tasks>

<verification>
- `grep "output.*standalone" next.config.ts` confirms standalone mode
- `grep -r "runtime = 'edge'" src/app/api/` returns zero matches
- Dockerfile exists with 3 FROM stages and non-root USER
- .dockerignore exists and excludes node_modules, .next, .env
</verification>

<success_criteria>
- Next.js configured for standalone output
- Production Dockerfile ready for Cloud Run deployment
- All API routes on Node.js runtime (no Edge Runtime)
- Docker build context optimized with .dockerignore
</success_criteria>

<output>
After completion, create `.planning/phases/02-cloud-run-api-gateway/02-01-SUMMARY.md`
</output>
