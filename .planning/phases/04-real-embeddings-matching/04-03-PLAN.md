---
phase: 04-real-embeddings-matching
plan: 03
type: execute
wave: 2
depends_on: ["04-01", "04-02"]
files_modified:
  - src/services/hybridMatchService.ts
  - src/services/matchService.ts
  - src/app/api/v1/match/semantic/route.ts
  - scripts/seed-artist-embeddings.ts
autonomous: true

must_haves:
  truths:
    - "hybridMatchService uses real Vertex AI embeddings instead of Math.sin mock"
    - "matchService uses Firestore vector search instead of Supabase pgvector"
    - "RRF fusion uses configurable weights from Firestore config document"
    - "Seed script generates and stores real embeddings for all artists in Neo4j"
    - "Search for 'Japanese traditional' returns semantically similar results to 'Japanese old-school'"
  artifacts:
    - path: "src/services/hybridMatchService.ts"
      provides: "Hybrid matching with real Vertex AI embeddings"
      exports: ["findMatchingArtists"]
    - path: "src/services/matchService.ts"
      provides: "Match orchestration with Firestore vector search"
      exports: ["getHybridArtistMatches"]
    - path: "scripts/seed-artist-embeddings.ts"
      provides: "Batch embedding generation for all artists"
  key_links:
    - from: "src/services/hybridMatchService.ts"
      to: "src/services/vertex-embedding-service.ts"
      via: "getQueryEmbedding import replacing generateQueryEmbedding"
      pattern: "import.*getQueryEmbedding.*vertex-embedding-service"
    - from: "src/services/hybridMatchService.ts"
      to: "src/services/firestore-vector-service.ts"
      via: "searchSimilarArtists import replacing Supabase searchSimilar"
      pattern: "import.*searchSimilarArtists.*firestore-vector-service"
    - from: "src/services/matchService.ts"
      to: "src/services/match-config-service.ts"
      via: "getMatchConfig + weightedRRF replacing hardcoded RRF"
      pattern: "import.*weightedRRF.*match-config-service"
    - from: "scripts/seed-artist-embeddings.ts"
      to: "src/services/vertex-embedding-service.ts"
      via: "batchGenerateEmbeddings for all artists"
      pattern: "batchGenerateEmbeddings"
---

<objective>
Rewire the hybrid matching pipeline to use real Vertex AI embeddings and Firestore vector search, and create the seed script to generate embeddings for all existing artists.

Purpose: This is the integration plan that connects all the foundation services (Plans 01+02) into the working matching pipeline. After this plan, hybrid matching works end-to-end with real data — the core deliverable of Phase 4.

Output: Rewired hybridMatchService.ts + matchService.ts using real embeddings, seed script for batch embedding generation.
</objective>

<execution_context>
@/Users/maryobrien/.claude/get-shit-done/workflows/execute-plan.md
@/Users/maryobrien/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-real-embeddings-matching/04-RESEARCH.md
@.planning/phases/04-real-embeddings-matching/04-01-SUMMARY.md
@.planning/phases/04-real-embeddings-matching/04-02-SUMMARY.md
@src/services/hybridMatchService.ts
@src/services/matchService.ts
@src/services/neo4jService.ts
@src/app/api/v1/match/semantic/route.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1a: Rewire hybridMatchService to use real Vertex AI embeddings and Firestore vector search</name>
  <files>src/services/hybridMatchService.ts</files>
  <action>
**hybridMatchService.ts** — Replace mock embeddings with real Vertex AI:

1. Remove import of `searchSimilar` from `./vectorDbService` and `VECTOR_DB_CONFIG` from `../config/vectorDbConfig.js`.
2. Add imports:
   - `getQueryEmbedding` from `./vertex-embedding-service`
   - `searchSimilarArtists` from `./firestore-vector-service`
   - `getMatchConfig, weightedRRF` from `./match-config-service`
3. Replace `generateQueryEmbedding()` function (the Math.sin mock at lines ~143-160):
   - Delete the entire function
   - In `executeVectorSearch()`, replace `await generateQueryEmbedding(query)` with `await getQueryEmbedding(query)`
4. Replace `searchSimilar(queryEmbedding, topK)` call in `executeVectorSearch()` with `searchSimilarArtists(queryEmbedding, topK)`. Map results to match expected `VectorSearchResult` format: `{ id: result.artistId, visualSimilarity: result.score, source: 'vector' }`.
5. In `findMatchingArtists()`:
   - At the top, call `const matchConfig = await getMatchConfig();`
   - Replace the hardcoded `CONFIDENCE_THRESHOLD = 0.5` with `matchConfig.confidenceThreshold`
   - After getting vectorResults and graphResults, replace the call to `mergeResults()` with proper RRF fusion:
     - Build rank lists from vectorResults and graphResults
     - Call `weightedRRF(graphRankList, vectorRankList, matchConfig)` to get RRF scores
     - Use RRF scores in composite score calculation
6. Update the timeout from `TIMEOUT_MS = 500` to `TIMEOUT_MS = 3000` (3 seconds — MATCH-06 success criteria allows <3s, and real embedding generation takes longer than mock).
7. Remove the `console.warn('[HybridMatch] Using mock embedding')` log line.
8. Keep the in-memory query cache (5min TTL) — it still applies to full results.
  </action>
  <verify>
- `npx tsc --noEmit src/services/hybridMatchService.ts` compiles without errors
- No imports from `./vectorDbService` in hybridMatchService.ts
- No `Math.sin` in hybridMatchService.ts: `grep -r "Math.sin" src/services/hybridMatchService.ts` returns nothing
- Imports vertex-embedding-service and firestore-vector-service: `grep "vertex-embedding-service\|firestore-vector-service" src/services/hybridMatchService.ts` returns both
  </verify>
  <done>
- hybridMatchService uses getQueryEmbedding (real Vertex AI) instead of Math.sin mock
- hybridMatchService uses searchSimilarArtists (Firestore) instead of Supabase searchSimilar
- RRF fusion in hybridMatchService uses configurable weights from Firestore config document
- No references to vectorDbService or mock embeddings remain in hybridMatchService
  </done>
</task>

<task type="auto">
  <name>Task 1b: Rewire matchService and API route to use Firestore vector search and configurable RRF</name>
  <files>src/services/matchService.ts, src/app/api/v1/match/semantic/route.ts</files>
  <action>
**matchService.ts** — Replace Supabase with Firestore vector search:

1. Remove import of `searchSimilar` from `./vectorDbService`.
2. Add imports:
   - `searchSimilarArtists` from `./firestore-vector-service`
   - `getMatchConfig, weightedRRF` from `./match-config-service`
3. In `getHybridArtistMatches()`:
   - Replace `supabaseMatches = await searchSimilar(embeddingVector, limit)` with `const firestoreMatches = await searchSimilarArtists(embeddingVector, limit)`. Map results: `{ id: result.artistId, score: result.score }`.
   - Rename `supabaseMatches` variable to `vectorMatches` throughout.
   - Rename `supabaseScoreMap` to `vectorScoreMap`.
   - Replace hardcoded `RRF_K = 60` with config from `getMatchConfig()`.
   - Replace `reciprocalRankFusion()` call with `weightedRRF()` from match-config-service.
4. Update log messages from `[MatchService] Supabase vector search` to `[MatchService] Firestore vector search`.

**src/app/api/v1/match/semantic/route.ts** — Minor cleanup:
- The route imports `findMatchingArtists` from `@/services/hybridMatchService` — this import stays the same (function signature unchanged).
- No functional changes needed, but update the `@ts-ignore` comment to a regular import if it now resolves correctly.
  </action>
  <verify>
- `npx tsc --noEmit src/services/matchService.ts src/app/api/v1/match/semantic/route.ts` compiles without errors
- No imports from `./vectorDbService` in matchService.ts
- `grep -ri "supabase" src/services/matchService.ts` returns nothing
- matchService imports from firestore-vector-service and match-config-service
  </verify>
  <done>
- matchService uses Firestore vector search instead of Supabase
- matchService uses configurable RRF weights from match-config-service
- All "supabase" references renamed to "vector" in matchService
- API route compiles cleanly with rewired imports
  </done>
</task>

<task type="auto">
  <name>Task 2: Create seed script to generate and store embeddings for all artists</name>
  <files>scripts/seed-artist-embeddings.ts</files>
  <action>
Create `scripts/seed-artist-embeddings.ts` — standalone script runnable via `npx tsx scripts/seed-artist-embeddings.ts`.

Purpose: Read all artists from Neo4j, generate text embeddings via Vertex AI, store in Firestore `artist_embeddings` collection.

Implementation:
1. Initialize firebase-admin (check `GOOGLE_APPLICATION_CREDENTIALS` env var for service account).
2. Initialize Neo4j driver directly (not via proxy — this is a server-side script):
   - Use `neo4j-driver` with env vars `NEO4J_URI`, `NEO4J_USERNAME`/`NEO4J_USER`, `NEO4J_PASSWORD`.
3. Query all artists: `MATCH (a:Artist) RETURN a.id AS id, a.name AS name, a.city AS city, a.styles AS styles, a.tags AS tags, a.bodyParts AS bodyParts LIMIT 500`.
4. For each artist, build embedding text: `"${name}. Located in ${city}. Specializes in: ${styles.join(', ')}. Tags: ${tags.join(', ')}. Body parts: ${bodyParts.join(', ')}"`. Skip null/undefined fields.
5. Batch embed in groups of 5 using `batchGenerateEmbeddings()` from vertex-embedding-service.
6. Store each embedding using `storeArtistEmbedding()` from firestore-vector-service.
7. Add progress logging: `Embedded ${count}/${total} artists`.
8. Add error handling per batch (don't fail entire script if one batch fails — log and continue).
9. Add summary at end: `Completed: ${success}/${total} artists embedded, ${errors} errors`.
10. Add `--dry-run` flag support: when passed, logs what would be embedded but doesn't call Vertex AI or write to Firestore.
11. Close Neo4j driver at end.

Add npm script to package.json: `"seed:embeddings": "tsx scripts/seed-artist-embeddings.ts"`.

Note: This script requires `GCP_PROJECT_ID`, `GOOGLE_APPLICATION_CREDENTIALS`, `NEO4J_URI`, `NEO4J_USERNAME`, `NEO4J_PASSWORD` env vars to be set. It's meant to be run manually during deployment, not as part of the app.
  </action>
  <verify>
- `npx tsc --noEmit scripts/seed-artist-embeddings.ts` compiles (or verify with `npx tsx --check scripts/seed-artist-embeddings.ts` if available)
- `npx tsx scripts/seed-artist-embeddings.ts --dry-run` runs without errors (logs but doesn't write)
- Script imports batchGenerateEmbeddings and storeArtistEmbedding
  </verify>
  <done>
- Seed script reads all artists from Neo4j
- Builds descriptive text per artist for embedding
- Generates real Vertex AI embeddings in batches of 5
- Stores normalized embeddings in Firestore artist_embeddings collection
- Supports --dry-run for safe testing
- npm script "seed:embeddings" added to package.json
  </done>
</task>

</tasks>

<verification>
- `grep -r "Math.sin" src/services/` returns no results in matching files
- `grep -r "supabase" src/services/matchService.ts` returns no results
- hybridMatchService.ts imports from vertex-embedding-service and firestore-vector-service
- matchService.ts imports from firestore-vector-service and match-config-service
- scripts/seed-artist-embeddings.ts exists and imports from vertex-embedding-service
- `npm run build` succeeds
- **Automated semantic similarity check (MATCH-02):** After the seed script runs, execute the following verification script inline (via `npx tsx -e`):
  ```
  npx tsx -e "
    const { generateTextEmbedding } = require('./src/services/vertex-embedding-service');
    const { normalizeVector } = require('./src/lib/embedding-normalization');
    async function verify() {
      const e1 = await generateTextEmbedding('Japanese traditional tattoo style');
      const e2 = await generateTextEmbedding('Japanese old-school tattoo style');
      const e3 = await generateTextEmbedding('watercolor abstract painting');
      const cosine = (a, b) => a.reduce((s, v, i) => s + v * b[i], 0);
      const simRelated = cosine(e1, e2);
      const simUnrelated = cosine(e1, e3);
      console.log('Related similarity (Japanese traditional vs old-school):', simRelated);
      console.log('Unrelated similarity (Japanese traditional vs watercolor):', simUnrelated);
      if (simRelated < 0.8) throw new Error('MATCH-02 FAIL: Related queries similarity ' + simRelated + ' < 0.8');
      if (simRelated <= simUnrelated) throw new Error('MATCH-02 FAIL: Related queries not more similar than unrelated');
      console.log('MATCH-02 PASS: Semantic similarity verified');
    }
    verify().catch(e => { console.error(e.message); process.exit(1); });
  "
  ```
  This verifies that Vertex AI text-embedding-004 produces cosine similarity > 0.8 for semantically related queries ("Japanese traditional" ~ "Japanese old-school") and that related queries are more similar than unrelated ones. This MUST pass before Plan 04-04 checkpoint.
</verification>

<success_criteria>
- Complete matching pipeline uses real Vertex AI embeddings end-to-end
- No mock/fake data in matching pipeline (MATCH-06)
- Seed script can populate all artist embeddings from Neo4j data (MATCH-03)
- RRF weights are configurable from Firestore (MATCH-04)
- Semantic search possible: same embedding model for queries and artists enables meaningful similarity (MATCH-02)
</success_criteria>

<output>
After completion, create `.planning/phases/04-real-embeddings-matching/04-03-SUMMARY.md`
</output>
