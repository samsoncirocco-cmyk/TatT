---
phase: 04-real-embeddings-matching
plan: 04
type: execute
wave: 3
depends_on: ["04-03"]
files_modified:
  - src/services/neo4jService.ts
autonomous: false

must_haves:
  truths:
    - "Neo4j queries in neo4jService.ts include LIMIT $limit with default 50"
    - "Matching completes in under 3 seconds end-to-end"
    - "No mock/fake data in the matching pipeline"
    - "Search for 'Japanese traditional' returns relevant artists"
  artifacts:
    - path: "src/services/neo4jService.ts"
      provides: "Neo4j service with pagination defaults"
  key_links:
    - from: "src/services/neo4jService.ts"
      to: "src/app/api/neo4j/query/route.ts"
      via: "Cypher queries with LIMIT parameter"
      pattern: "LIMIT \\$limit"
---

<objective>
Apply pagination defaults to Neo4j service queries and verify the entire matching pipeline works end-to-end.

Purpose: Ensure all Neo4j queries have LIMIT 50 pagination (MATCH-05) and validate that the complete hybrid matching pipeline — from user query to ranked artist results — works with real data.

Output: Updated neo4jService.ts with pagination, end-to-end verification of matching.
</objective>

<execution_context>
@/Users/maryobrien/.claude/get-shit-done/workflows/execute-plan.md
@/Users/maryobrien/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-real-embeddings-matching/04-RESEARCH.md
@.planning/phases/04-real-embeddings-matching/04-01-SUMMARY.md
@.planning/phases/04-real-embeddings-matching/04-02-SUMMARY.md
@.planning/phases/04-real-embeddings-matching/04-03-SUMMARY.md
@src/services/neo4jService.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add pagination defaults to all Neo4j service queries</name>
  <files>src/services/neo4jService.ts</files>
  <action>
Update `src/services/neo4jService.ts` to enforce LIMIT 50 pagination on all queries:

1. Import `NEO4J_DEFAULT_LIMIT` from `../lib/neo4j`.
2. In `findMatchingArtists()`: the existing query already has `LIMIT 20`. Change to `LIMIT $limit` and pass `limit: Math.min(preferences.limit || NEO4J_DEFAULT_LIMIT, NEO4J_DEFAULT_LIMIT)` in params. This ensures limit never exceeds 50 even if caller passes higher.
3. In `findArtistMatchesForPulse()`: same pattern. Already has `LIMIT $limit` — add `Math.min(limit, NEO4J_DEFAULT_LIMIT)` guard on the limit parameter passed to params.
4. In `getArtistsByIds()`: add `LIMIT $limit` to the query with `limit: NEO4J_DEFAULT_LIMIT`. Currently has no limit.
5. In `getArtistGenealogy()`: the mentor chain query uses `*1..5` depth limit which is fine. No LIMIT needed on single-artist lookup.
6. In `getInfluencedArtists()`: add `LIMIT $limit` with `limit: NEO4J_DEFAULT_LIMIT`.
7. In `findArtistsByEmbeddingIds()`: add `LIMIT $limit` with `limit: NEO4J_DEFAULT_LIMIT`.
  </action>
  <verify>
- `npx tsc --noEmit src/services/neo4jService.ts` compiles without errors
- All multi-result queries have LIMIT in their Cypher
- `grep -c "LIMIT" src/services/neo4jService.ts` returns at least 5
  </verify>
  <done>
- All Neo4j queries that return multiple results have LIMIT $limit with max 50
- Single-entity lookups (getArtistById, getArtistGenealogy) are unchanged
- Callers cannot exceed LIMIT 50 even if they pass higher values
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify hybrid matching works end-to-end</name>
  <files>src/services/hybridMatchService.ts</files>
  <action>
Human verification checkpoint. No automated implementation — verify the complete hybrid matching pipeline works end-to-end with real Vertex AI embeddings, Firestore vector search, configurable RRF weights, Neo4j with timeouts and pagination.
  </action>
  <verify>
1. Ensure environment variables are set: `GCP_PROJECT_ID`, `GOOGLE_APPLICATION_CREDENTIALS`, `NEO4J_URI`, `NEO4J_USERNAME`, `NEO4J_PASSWORD`
2. Run the seed script: `npm run seed:embeddings -- --dry-run` to verify it can read artists from Neo4j
3. If Neo4j has artists: run `npm run seed:embeddings` to generate real embeddings
4. Start dev server: `npm run dev`
5. Test the semantic match API:
   ```bash
   curl -X POST http://localhost:3000/api/v1/match/semantic \
     -H "Content-Type: application/json" \
     -H "Authorization: Bearer $VITE_FRONTEND_AUTH_TOKEN" \
     -d '{"query": "Japanese traditional", "max_results": 5}'
   ```
6. Verify response has `matches` array with artists (not empty)
7. Verify `performance.duration_ms` is under 3000
8. Test semantic similarity: run same query with "Japanese old-school" — results should overlap significantly with "Japanese traditional"
9. Check Firestore console: `config/matching` document should be readable (or not exist, with defaults applied)
10. Check Firestore console: `artist_embeddings` collection should have documents with `embedding` vector fields
  </verify>
  <done>
- Matching API returns real results from Vertex AI embeddings (not mock)
- Performance under 3 seconds end-to-end
- Semantic similarity works: related queries return overlapping results
- Type "approved" or describe issues to continue
  </done>
</task>

</tasks>

<verification>
- All Neo4j queries have LIMIT with max 50
- Matching API returns real results (not mock)
- Performance under 3 seconds
- No Math.sin or Supabase references in matching pipeline
</verification>

<success_criteria>
- Search "Japanese traditional" returns same/similar artists as "Japanese old-school" (MATCH-02)
- Top 5 matches are relevant to query (MATCH-06)
- Matching completes in <3 seconds end-to-end (MATCH-06)
- No mock/fake data in matching pipeline (MATCH-01, MATCH-06)
- Neo4j queries have 5s timeout and LIMIT 50 pagination (MATCH-05)
- RRF weights configurable via Firestore (MATCH-04)
- Artist embeddings generated and stored (MATCH-03)
</success_criteria>

<output>
After completion, create `.planning/phases/04-real-embeddings-matching/04-04-SUMMARY.md`
</output>
